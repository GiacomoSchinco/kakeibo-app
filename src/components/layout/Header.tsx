import React from "react";

const Header: React.FC = () => (
  <header className="w-full bg-gray-100 p-4 shadow">
    <h1 className="text-xl font-bold">Kakeibo App</h1>
  </header>
);

export default Header;
